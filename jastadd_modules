include("extendj/jastadd_modules") // Include the core ExtendJ modules.

module "muli-lang", { // TODO Replace with your own module name.

	imports "java8 frontend"

	java {
		basedir "src/java/"
		include "**/*.java"
	}

	jastadd {
		basedir "src/jastadd/"
		include "**/*.ast"
		include "**/*.jadd"
		include "**/*.jrag"
	}

	scanner {
		basedir "src/scanner/"
		include "**/*.flex"
	}

	parser {
		basedir "src/parser/"
		include "**/*.parser"
	}
}


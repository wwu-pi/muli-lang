public class MuliTest {
    public int whatisthis
    free, another
            free, fixednonfree;

    private void testfun() {
        int x free, y = 0, z;
        boolean abc free;
        // boolean abcd free = false;  Forbidden by compiler :)
        // boolean abc = true; // TODO: redeclaration is currently possible in extendj! => report

        while (y < 5) {
            //if (!abc) continue;
            int a = 5 - y - 1;
            break;
        }

        // while (y < 6) int x free; Not possible! :)
    }

    public void moretest() {
        String f free;
    }

    public MuliTest() {
        Object z free;
    }
}